<resources>
    <style name="ActionBarTheme" parent="Theme.AppCompat.Light">
        <item name="android:windowBackground">@drawable/legato_bg</item>
        <item name="checkboxStyle">@style/legato_checkbox</item>
    </style>

    <!-- Added because of popup filter start-->
    <style name="AppTheme.FilterIcon">
        <item name="android:layout_width">30dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:layout_marginLeft">8dp</item>
    </style>

    <style name="AppTheme.FilterSpinner" parent="Base.Widget.AppCompat.Spinner.Underlined">
            <item name="android:layout_width">match_parent</item>
            <item name="android:layout_height">wrap_content</item>
            <item name="android:paddingBottom">16dp</item>
            <item name="android:layout_marginBottom">4dp</item>
    </style>

    <style name="NoActionBarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/legato_bg</item>
        <item name="checkboxStyle">@style/legato_checkbox</item>
    </style>

    <style name="ListFont" parent="@android:style/Widget.ListView">
        <item name="android:textColor">@color/activity_background</item>
    </style>

    <style name="legato_checkbox" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:textColor">@color/selector_text</item>
    </style>

    <style name="ThemeOverlay.FilterButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/colorPrimary</item>
    </style>

    <!-- fragment_user_profile; item_spotify_search -->
    <style name="stdButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">36dp</item>
        <item name="android:layout_margin">@dimen/std_marginSmall</item>
        <item name="android:textSize">12sp</item>
        <item name="android:padding">2dp</item>
    </style>

    <!-- fragment_user_profile; item_nearby_user -->
    <style name="availability">
        <item name="android:layout_width">20dp</item>
        <item name="android:layout_height">20dp</item>
        <item name="android:layout_margin">@dimen/std_marginSmall</item>
    </style>

    <!-- item_nearbyuser -->>
    <style name="socialMedia">
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:layout_margin">@dimen/std_marginSmall</item>
    </style>

    <!-- userprofilefragment -->>
    <style name="socialMediaBig">
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:layout_margin">@dimen/std_marginSmall</item>
    </style>

    <!-- item_skill; fragment_band_basic_info; fragment_filters -->
    <style name="seekBar">
        <item name="android:layout_width">194dp</item>
        <item name="android:layout_height">20dp</item>
        <item name="android:layout_margin">@dimen/std_margin</item>
    </style>

    <!-- item_nearbyuser; item_spotify_search -->
    <style name="iconImage">
        <item name="android:layout_width">68dp</item>
        <item name="android:layout_height">68dp</item>
        <item name="android:layout_margin">@dimen/std_margin</item>
    </style>

</resources>
